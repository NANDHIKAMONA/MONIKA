#include<stdio.h>
int main()
{
int array [50],size,i, largest;
printf("entr the size of the array"):
scanf("%d,&size):
printf(" enter %d element of the array:",size):
for(i=0;i<size;i++)
scanf("%d";&array[i]);
for(i=1;i<size;i++)
{
if(smallest<array[i])
smallest=array[i];
}
printf(" largest element present in a given array:%d",smallest);
return 0;
}
